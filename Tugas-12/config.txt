server {
        server_name dikytest.duckdns.org www.dikytest.duckdns.org;

        root /var/www/html;
        index index.html index.htm;
        #location / {
        #       proxy_pass http://localhost:8080;
        #       proxy_set_header X-Real-IP $remote_addr;
        #       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #       proxy_set_header X-Forwarded-Proto $scheme;
        #}

        location / {
                try_files $uri $uri/ /index.html;
        }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/dikytest.duckdns.org-0001/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/dikytest.duckdns.org-0001/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot



}
server {
    if ($host = www.dikytest.duckdns.org) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    if ($host = dikytest.duckdns.org) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


        listen 80;
        server_name dikytest.duckdns.org www.dikytest.duckdns.org;
    return 404; # managed by Certbot




}
